#Password generator
import random
import string

#setting no of password character
length = int(input("Enter password of desired length: "))

#characters to make password 
characters = string.ascii_letters + string.digits + string.punctuation

password = ''.join(random.choice(characters) for _ in range(length))

# create a secure password
print("\ncreated password:")
print(password)
